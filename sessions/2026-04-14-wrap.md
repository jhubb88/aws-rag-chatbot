# Session Wrap-Up — 2026-04-14 (Phase 2)

## What Was Completed

### Infrastructure
- Added `IngestLambda` (Python 3.12, 512MB, 300s) to CloudFormation template
- Added `IngestLambdaRole` — least-privilege IAM: S3 read on `documents/*`,
  read/write on `documents/index.json`, Bedrock InvokeModel on Titan Embeddings v2 only,
  CloudWatch Logs write scoped to `${IngestLambdaLogGroup.Arn}:*`
- Added `IngestLambdaLogGroup` — `/aws/lambda/rag-chatbot-ingest-dev`, 30-day retention
- Added `IngestLambdaS3Permission` — S3 principal allowed to invoke (manual wiring only)
- Stack `rag-knowledge-chatbot` redeployed successfully with `CAPABILITY_NAMED_IAM`
- Lambda zip packaged locally (pypdf + handler.py, 297KB) and uploaded to
  `s3://rag-chatbot-603509861186-dev/lambda/ingest.zip`

### Code
- `src/lambdas/ingest/handler.py` — ingest Lambda: S3 read, chunking (~500w/50w overlap),
  Bedrock Titan Embeddings v2 embed, merge-safe JSON index write to S3
- `src/lambdas/ingest/requirements.txt` — pypdf==4.3.1 only
- `scripts/smoke_test_ingest.py` — manual smoke test (not automated coverage);
  mutates live S3 demo data; clearly documented as such

### Test Gate
- **PASSED** — Bedrock Titan Embeddings v2 is live and unblocked
- End-to-end confirmed: txt read → chunk → embed → index.json written to S3
- First embedding vector confirmed real (1536-dim, values non-zero)

### Codex Reviews
- handler.py: approved for demo — not production-grade but coherent and demonstrably works
- smoke test: flagged as manual smoke script, not automated test coverage — renamed to `scripts/`
- CloudFormation additions: two medium fixes applied before writing:
  1. CloudWatch Logs resource changed to `${Arn}:*` to cover log streams
  2. `DependsOn: RagChatbotBucket` added with clear prerequisite comment

## What Was Skipped
- S3 bucket event notification (circular dependency in CloudFormation — deferred; direct invoke is sufficient for demo)
- PDF ingest live test (pypdf is packaged but not tested end-to-end against a real PDF)
- Query Lambda (Phase 3)
- Frontend (Phase 4)

## Errors Hit
- `CAPABILITY_IAM` rejected on deploy — fixed to `CAPABILITY_NAMED_IAM` (named role in template)
- `boto3` not installed locally for smoke test — installed with `--break-system-packages`
- Stray markdown fence in smoke_test_ingest.py caused SyntaxError — removed

## Next Steps (Phase 3 — Query Lambda)
1. Write `src/lambdas/query/handler.py`:
   - Accept POST `{"query": "..."}` from API Gateway
   - Embed the query via Bedrock Titan Embeddings v2
   - Load `documents/index.json` from S3
   - Cosine similarity search — return top-K chunks
   - Route to Bedrock Claude (if unblocked) or Nebius Llama (live fallback)
   - Return answer + source citations
2. Wire query Lambda into API Gateway (replace MOCK integration)
3. Add query Lambda to CloudFormation template
4. Write `scripts/smoke_test_query.py`

## Commit / Tag
- Commit: `61ee46e` — feat: phase 2 document ingestion pipeline
- Tag: `v0.3-ingest`
- Branch: main

---

## Phase 1 Reference (prior session — 2026-04-14 earlier)

### Stack Outputs
| Resource | Value |
|---|---|
| S3 Bucket | `rag-chatbot-603509861186-dev` |
| Website URL | `http://rag-chatbot-603509861186-dev.s3-website-us-east-1.amazonaws.com` |
| API Endpoint | `https://uiauqskgv0.execute-api.us-east-1.amazonaws.com/dev/query` |
| Lambda Role ARN | `arn:aws:iam::603509861186:role/rag-chatbot-lambda-role-dev` |
| Log Group | `/aws/lambda/rag-chatbot-query-dev` |

- Commit: `68a92b6` — Tag: `v0.2-infra`
