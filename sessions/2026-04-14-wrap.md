# Session Wrap-up — 2026-04-14

## What Was Done

### AWS Budgets (Step 2)
- Created `rag-chatbot-alert-15`: MONTHLY cost budget, alerts at $15 actual spend
- Created `rag-chatbot-alert-18`: MONTHLY cost budget, alerts at $18 actual spend
- Both notify jimmyhubbard.0813@gmail.com via AWS Budgets email notification (no SNS)
- **Note:** portfolio-user initially lacked `budgets:ModifyBudget` — IAM permission was
  added manually in the console before the commands succeeded.

### CloudFormation Template (Step 3)
- Created: `infrastructure/cloudformation/template.yaml`
- Resources provisioned (on deploy):
  - S3 bucket: `rag-chatbot-{accountId}-{env}` — versioned, static website enabled,
    public read scoped to `/frontend/*`, CloudTrail write allowed
  - CloudWatch Log Group: `/aws/lambda/rag-chatbot-query-{env}`, 30-day retention
  - Lambda execution role: `rag-chatbot-lambda-role-{env}` — permissions for
    CloudWatch Logs, S3, SSM (`/rag-chatbot/*`), Bedrock Titan Embeddings
  - API Gateway REST API: POST /query with MOCK integration (Phase 2 placeholder),
    OPTIONS /query for CORS, stage named per Environment parameter
  - CloudTrail: `rag-chatbot-trail-{env}`, logs to S3 under `cloudtrail/` prefix
- Template validated: `aws cloudformation validate-template` returned clean

### Git (Step 4)
- Committed: `580660d` — "feat: add cloudformation template and budget setup"
- NOT pushed — awaiting explicit instruction

## What Was Skipped
- CloudFormation stack deployment — intentionally deferred (template + validation only this session)
- Lambda function code — Phase 2
- Bedrock live calls — Bedrock is blocked at account level (Support case open)

## Errors Hit
- `budgets:ModifyBudget` AccessDeniedException on first budget attempt
  - Fix: portfolio-user IAM policy updated in console to allow `budgets:CreateBudget`,
    `budgets:ModifyBudget`, `budgets:ViewBudget`

## Exact Next Steps (Phase 1 completion)

1. **Push to GitHub** (when ready):
   ```
   git push origin main
   ```

2. **Deploy the CloudFormation stack**:
   ```bash
   aws cloudformation create-stack \
     --stack-name rag-chatbot-dev \
     --template-body file://infrastructure/cloudformation/template.yaml \
     --capabilities CAPABILITY_NAMED_IAM \
     --profile portfolio-user
   ```
   - `CAPABILITY_NAMED_IAM` is required (named IAM role in template)
   - Monitor: `aws cloudformation describe-stack-events --stack-name rag-chatbot-dev --profile portfolio-user`

3. **Tag Phase 1 complete** after successful stack deployment:
   ```bash
   git tag v0.2-infrastructure && git push origin v0.2-infrastructure
   ```

4. **Phase 2 begins** once Bedrock Support case resolves:
   - Write Lambda query handler (`src/lambda/query/handler.py`)
   - Wire Lambda into API Gateway (replace MOCK integration)
   - Build embeddings ingestion pipeline

## Stack Outputs (available after deploy)
- `BucketName` — S3 bucket name
- `BucketWebsiteURL` — Frontend hosting URL
- `ApiEndpoint` — POST /query URL
- `LambdaRoleArn` — Role ARN for Phase 2 Lambda attachment
- `LogGroupName` — CloudWatch log group
