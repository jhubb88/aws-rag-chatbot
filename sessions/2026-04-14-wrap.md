# Session Wrap-up — 2026-04-14

## What Was Done

### AWS Budgets (Step 2) — prior session
- Created `rag-chatbot-alert-15`: MONTHLY cost budget, alerts at $15 actual spend
- Created `rag-chatbot-alert-18`: MONTHLY cost budget, alerts at $18 actual spend
- Both notify jimmyhubbard.0813@gmail.com via AWS Budgets email notification (no SNS)

### CloudFormation Template (Step 3) — prior session
- Created: `infrastructure/cloudformation/template.yaml`
- Committed: `580660d` — "feat: add cloudformation template and budget setup"

---

### Phase 1 Completion — this session

#### Stack Deployed
- Stack `rag-knowledge-chatbot` deployed and live in `us-east-1`
- Tag: `v0.2-infra` pushed to `jhubb88/aws-rag-chatbot`
- Commit: `68a92b6`

#### Stack Outputs
| Resource | Value |
|---|---|
| S3 Bucket | `rag-chatbot-603509861186-dev` |
| Website URL | `http://rag-chatbot-603509861186-dev.s3-website-us-east-1.amazonaws.com` |
| API Endpoint | `https://uiauqskgv0.execute-api.us-east-1.amazonaws.com/dev/query` |
| Lambda Role ARN | `arn:aws:iam::603509861186:role/rag-chatbot-lambda-role-dev` |
| Log Group | `/aws/lambda/rag-chatbot-query-dev` |

#### Template Changes Made
Two fixes were required to deploy cleanly:
1. Removed `LoggingLevel: INFO` and `DataTraceEnabled: false` from `ApiStage.MethodSettings` — requires account-level API Gateway → CloudWatch role that isn't configured.
2. Removed `Tags` blocks from `RagChatbotApi` and `ApiStage` — `portfolio-user` lacks `apigateway:PUT` on tag resources.

#### IAM Policies Added to portfolio-user
Three inline policies were added during this session:
- `ApiGatewayManage` — `apigateway:*` on `arn:aws:apigateway:us-east-1::/*`
- `CloudWatchLogsManage` — logs create/delete/describe/tag on `arn:aws:logs:us-east-1:603509861186:log-group:*`
- `RagChatbotDeployPolicy` — S3, IAM role, CloudTrail scoped to `rag-chatbot-*` resources

## What Was Skipped
- Phase 2 (ingestion pipeline) — intentionally deferred until Phase 1 confirmed live
- Lambda function code — Phase 2
- Bedrock live calls — blocked at account level (AWS Support case open)

## Errors Hit
- Multiple `portfolio-user` IAM permission gaps on first deploy attempts (`apigateway:PUT`, `apigateway:POST`, `logs:DeleteLogGroup`, `iam:CreateRole`) — resolved by adding inline policies above.

## Exact Next Steps (Phase 2)
Phase 2 is the document ingestion pipeline:
1. Create `data/documents/` directory (gitignored)
2. Write `src/lambdas/ingest/handler.py`:
   - Accept PDF/text from `data/documents/`
   - Chunk into ~500 token segments with overlap
   - Call Titan Embeddings v2 via Bedrock to embed each chunk
   - Store chunks + embeddings as JSON vector index in S3 (`documents/` prefix)
3. Package and deploy ingest Lambda via CloudFormation update
4. Note: Bedrock still blocked at account level — embeddings code path write-only until AWS Support resolves
