# Session Wrap-Up — 2026-04-16 (Phase 5: Observability + Model Migration)

## What Was Completed

### Model Migration
- Updated query Lambda `HAIKU_MODEL` constant from `anthropic.claude-3-haiku-20240307-v1:0` (EOL 2026-09-10) to `us.anthropic.claude-haiku-4-5-20251001-v1:0` (cross-region inference profile)
- Cleaned module docstring and `_generate_bedrock()` — removed all stale BLOCKED/WARN language
- Improved `AccessDeniedException` handler to log the real AWS error message before returning the user-facing fallback
- Removed the TODO comment about model migration

### IAM Updates (synced into CloudFormation)
- Added `BedrockHaikuInferenceProfile` IAM statement to `QueryLambdaRole`:
  - `bedrock:InvokeModel` on both the inference profile ARN and the foundation model ARN
- Added `MarketplaceModelAccess` statement (`aws-marketplace:ViewSubscriptions`, `aws-marketplace:Subscribe`)
- Both statements now live in `infrastructure/cloudformation/template.yaml` — in sync with what was previously applied via CLI

### Phase 5 CloudFormation Resources
- `RagChatbotAlertsTopic` — SNS topic `RAG-Chatbot-Alerts-dev`
- `RagChatbotAlertsSubscription` — email to jimmy.hubbard0813@gmail.com (**confirmed by Jimmy**)
- `QueryErrorRateAlarm` — metric math alarm, error rate >5%, TreatMissingData: notBreaching
- `QueryDurationAlarm` — p95 duration >10,000ms, TreatMissingData: notBreaching
- `RagChatbotDashboard` — `RAG-Chatbot-Dashboard` with 4 widgets (invocations, errors, p95 latency, estimated cost)
- `RagChatbotCloudFront` — distribution `EN88LEBW14923`, S3 website CustomOriginConfig (HTTP), HTTPS redirect, `DefaultRootObject: frontend/index.html`
- New stack outputs: `AlertsTopicArn`, `CloudFrontURL`

### Stack Deploy
- `aws cloudformation deploy` succeeded — stack `rag-knowledge-chatbot` updated
- All new resources created on first deploy, no rollbacks

### Test Gate — Results
| Check | Result |
|---|---|
| CloudWatch dashboard `RAG-Chatbot-Dashboard` visible | ✅ |
| Alarm `rag-chatbot-error-rate-dev` in OK state | ✅ |
| Alarm `rag-chatbot-p95-duration-dev` in OK state | ✅ |
| CloudFront `https://d1r1qv7io7k8vk.cloudfront.net` returns 200 | ✅ |
| SNS subscription confirmed (email click) | ✅ |
| Nebius engine (`selected_engine: nebius`) working | ✅ |
| Bedrock smoke test (`selected_engine: bedrock`) | ❌ — see open items |

## Open Items

### Bedrock Smoke Test — Blocked (marketplace subscription)
Smoke test run against `https://uiauqskgv0.execute-api.us-east-1.amazonaws.com/dev/query`:
```json
{"query": "What is the NTCIP simulator?", "selected_engine": "bedrock"}
```
Response: `engine_used: bedrock_blocked`

Lambda logs show:
```
[ERROR] Bedrock AccessDeniedException — AWS: Model access is denied due to IAM user or service role
is not authorized to perform the required AWS Marketplace actions (aws-marketplace:ViewSubscriptions,
aws-marketplace:Subscribe) to enable access to this model. Your AWS Marketplace subscription for this
model cannot be completed at this time.
```

All IAM fixes are in place and model access was approved in the Bedrock console. This is an AWS-side marketplace subscription propagation failure. The `bedrock_blocked` fallback in the Lambda returns a clean user-facing message rather than a 500. No further action until AWS resolves the subscription.

## Infrastructure Delivered

| Resource | Name / Value |
|---|---|
| CloudFront URL | `https://d1r1qv7io7k8vk.cloudfront.net` |
| CloudFront distribution ID | `EN88LEBW14923` |
| CloudWatch dashboard | `RAG-Chatbot-Dashboard` |
| Alarm: error rate | `rag-chatbot-error-rate-dev` (>5%) |
| Alarm: p95 duration | `rag-chatbot-p95-duration-dev` (>10s) |
| SNS topic ARN | `arn:aws:sns:us-east-1:603509861186:RAG-Chatbot-Alerts-dev` |
| Bedrock model (migrated) | `us.anthropic.claude-haiku-4-5-20251001-v1:0` |
| Commit | `beae846` |
| Tag | `v0.7-observability` |

## Exact Next Steps

1. **Bedrock smoke test** — retry when AWS resolves the marketplace subscription. Command:
   ```bash
   curl -s -X POST https://uiauqskgv0.execute-api.us-east-1.amazonaws.com/dev/query \
     -H "Content-Type: application/json" \
     -d '{"query": "What is the NTCIP simulator?", "selected_engine": "bedrock"}'
   ```
   Confirm `engine_used: bedrock` in response.
2. **Phase 6** — Integration testing (not started).
3. **Phase 7** — Portfolio polish: "About This Demo" modal (see planning doc for spec).
