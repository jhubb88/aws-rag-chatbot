# Session Wrap-Up — 2026-04-16 (Phase 5: Observability + Model Migration)

## What Was Completed

### Model Migration (Step 1)
- Updated query Lambda `HAIKU_MODEL` from `anthropic.claude-3-haiku-20240307-v1:0` (EOL 2026-09-10) to `us.anthropic.claude-haiku-4-5-20251001-v1:0` (cross-region inference profile)
- Cleaned up module docstring and `_generate_bedrock()` — removed stale "BLOCKED/WARN" language
- Improved `AccessDeniedException` handler to log actual AWS error message
- Removed TODO comment about model migration

### IAM Updates (Step 1 support)
- Added `BedrockHaikuInferenceProfile` IAM statement to `QueryLambdaRole` in CloudFormation template:
  - `bedrock:InvokeModel` on inference profile ARN + foundation model ARN
- Added `MarketplaceModelAccess` IAM statement (`aws-marketplace:ViewSubscriptions`, `aws-marketplace:Subscribe`)
- Both now synced in CloudFormation template (previously only applied via CLI)

### Phase 5 CloudFormation Resources (Steps 2–5)
- `RagChatbotAlertsTopic` — SNS topic `RAG-Chatbot-Alerts-dev`
- `RagChatbotAlertsSubscription` — email to jimmy.hubbard0813@gmail.com (pending email click to confirm)
- `QueryErrorRateAlarm` — metric math alarm, error rate >5%, TreatMissingData: notBreaching
- `QueryDurationAlarm` — p95 duration >10000ms, TreatMissingData: notBreaching
- `RagChatbotDashboard` — `RAG-Chatbot-Dashboard` with 4 widgets
- `RagChatbotCloudFront` — distribution `EN88LEBW14923`, S3 website origin, HTTPS redirect
- New outputs: `AlertsTopicArn`, `CloudFrontURL`

### Stack Deploy
- `aws cloudformation deploy` succeeded — `rag-knowledge-chatbot` stack updated
- CloudFront status: `Deployed` immediately
- CloudFront URL: `https://d1r1qv7io7k8vk.cloudfront.net` — returns HTTP 200

### Test Gate
- CloudWatch dashboard `RAG-Chatbot-Dashboard` confirmed in console ✅
- Alarms `rag-chatbot-error-rate-dev` and `rag-chatbot-p95-duration-dev` both in OK state ✅
- CloudFront URL returns 200, serves frontend/index.html ✅

## What Was Skipped / Deferred

- **Bedrock smoke test**: Deferred pending AWS Marketplace subscription propagation for `claude-haiku-4-5-20251001`. Model access was approved in Bedrock console and marketplace permissions added to the role, but subscription was not confirmed in time. Jimmy said to circle back after propagation.
- **Synthetic alarm test**: Not explicitly tested; alarms confirmed created and in OK state.
- **SNS email confirmation**: Subscription is pending — Jimmy must click the AWS confirmation email.

## Errors Hit

1. **Bedrock ValidationException**: Bare model ID `anthropic.claude-haiku-4-5-20251001-v1:0` not supported with on-demand throughput. Fixed by switching to cross-region inference profile `us.anthropic.claude-haiku-4-5-20251001-v1:0`.

2. **Bedrock AccessDeniedException — IAM**: Lambda role lacked permissions for the inference profile ARN. Fixed via `aws iam put-role-policy` (then synced into CloudFormation template).

3. **Bedrock AccessDeniedException — Marketplace**: "IAM user or role not authorized to perform aws-marketplace:ViewSubscriptions, aws-marketplace:Subscribe." Added `MarketplaceModelAccess` IAM statement. Also required Jimmy to manually approve Claude Haiku 4.5 in Bedrock console. Still pending propagation.

4. **portfolio-user can't self-modify IAM**: `aws iam put-user-policy` returned AccessDenied. Jimmy manually added `RagChatbotPhase5Policy` inline policy in AWS Console (CloudWatch, SNS, CloudFront permissions for CloudFormation deploy).

## New Infrastructure

| Resource | Name / ID | Value |
|---|---|---|
| CloudFront distribution | `EN88LEBW14923` | `https://d1r1qv7io7k8vk.cloudfront.net` |
| CloudWatch dashboard | `RAG-Chatbot-Dashboard` | 4 widgets |
| Alarm: error rate | `rag-chatbot-error-rate-dev` | >5%, OK |
| Alarm: p95 duration | `rag-chatbot-p95-duration-dev` | >10s, OK |
| SNS topic | `RAG-Chatbot-Alerts-dev` | arn:aws:sns:us-east-1:603509861186:RAG-Chatbot-Alerts-dev |
| Haiku model | `us.anthropic.claude-haiku-4-5-20251001-v1:0` | cross-region inference profile |

## Exact Next Steps

1. **Confirm SNS email subscription** — click the link in the AWS confirmation email sent to jimmy.hubbard0813@gmail.com.
2. **Bedrock smoke test** — when marketplace subscription propagates, run a query via the frontend with Bedrock engine selected and confirm `engine_used: bedrock` in the response. Use:
   ```bash
   curl -X POST https://uiauqskgv0.execute-api.us-east-1.amazonaws.com/dev/query \
     -H "Content-Type: application/json" \
     -d '{"query": "What is the NTCIP simulator?", "selected_engine": "bedrock"}'
   ```
3. **Phase 6** — Integration testing (not started).
4. **Phase 7** — Portfolio polish: "About This Demo" modal, additional UI polish.
5. **Update portfolio CONTEXT.md** with Phase 5 complete + CloudFront URL.
