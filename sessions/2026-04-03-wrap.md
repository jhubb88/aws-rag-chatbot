# Session Wrap-Up — 2026-04-03

## Phase
Pre-build checklist — BLOCKED before Phase 1 could begin

## What Was Completed This Session
- Confirmed git repo and Phase 0 docs are intact
- Read session wrap-up, TEST_GATES.md, and ARCHITECTURE_DECISIONS.md
- Ran Bedrock access test for Claude 3 Haiku → AccessDeniedException
- Ran Bedrock access test for Titan Text Express v1 → end-of-life (ResourceNotFoundException)
- Ran bedrock:ListFoundationModels → AccessDeniedException
- Confirmed: portfolio-user (arn:aws:iam::603509861186:user/portfolio-user) has zero Bedrock IAM permissions
- Attempted Option 1 (IAM policy fix) manually — ran into unspecified problems in console
- Identified root cause: no identity-based policy allows any bedrock:* action for portfolio-user

## What Was NOT Finished and Why
- Pre-build checklist not completed — hard blocked on Bedrock access
- Phase 1 (CloudFormation) not started — Bedrock must be confirmed working before infrastructure is written
- All remaining checklist items (Titan Embeddings, Budget, CloudTrail, IAM review, git status) not run — no point until Bedrock is unblocked

## Errors Hit
- `bedrock:InvokeModel` on `anthropic.claude-3-haiku-20240307-v1:0` → AccessDeniedException — no identity-based policy
- `bedrock:InvokeModel` on `amazon.titan-text-express-v1` → ResourceNotFoundException — model is end-of-life
- `bedrock:ListFoundationModels` → AccessDeniedException — same IAM root cause
- Option 1 (adding inline IAM policy to portfolio-user) — attempted manually, encountered problems (details unknown)

## Root Cause
`portfolio-user` has no Bedrock permissions in any attached IAM policy. This may be due to a permission boundary, an SCP at the AWS Organization level, or a missing policy. An AWS Support case has been opened to resolve account-level Bedrock access.

## Exact Next Step
**Do not start Session 3 until AWS Support confirms Bedrock access is enabled.**

When support resolves the case:
1. Re-run the Bedrock test:
```
cat > /tmp/bt.json << 'EOF'
{"anthropic_version":"bedrock-2023-05-31","max_tokens":10,"messages":[{"role":"user","content":"hi"}]}
EOF

aws bedrock-runtime invoke-model \
  --model-id anthropic.claude-3-haiku-20240307-v1:0 \
  --body fileb:///tmp/bt.json \
  --region us-east-1 \
  /tmp/bo.json && cat /tmp/bo.json
```
2. If it passes: run Titan Embeddings v2 test (checklist item 2)
3. Continue through all 8 pre-build checklist items
4. Then begin Phase 1 — CloudFormation

## Decisions Made This Session
- No architecture decisions made — session was entirely diagnostic
- `amazon.titan-text-express-v1` is confirmed end-of-life — do not use it as a fallback
- Model swap to any Bedrock alternative is moot until IAM is fixed — the block is account-wide, not model-specific
